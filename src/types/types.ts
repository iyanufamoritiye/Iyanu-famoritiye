export type ProjectType = {
  image: string;
  projectName: string;
  company: string;
  title: string;
  link: string;
  description: string;
};

export type LanguagesType = {
  icon: string;
  alt: string;
};